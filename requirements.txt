# Environment variable management
python-dotenv>=1.0.0

# Web interface
flask
folium

# Maps, images, HTTP utilities
Pillow
requests

# AIS vessel feed (aisstream.io WebSocket)
websockets>=12.0

# Round touch display (1080×1080 FlightScnr UI)
pygame>=2.5.0
